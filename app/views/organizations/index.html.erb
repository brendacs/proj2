<h1>Task Organizer</h1>

<%= link_to 'Add New Organization', organization_new_path %>

<% @organizations.each do |o| %>
<div>
	<% @organizations.each do |org| %>
	<h2>Organization: <%= org.name %></h2>

	<div>
		<h4> Officers </h4>
		<%= link_to 'Add New Officer', officer_new_path %>
		<% @officers.each do |of| %>
		<p> Name: <%= of.name %></p>

		<div>
			<h4>Committee Members</h4>
			<%= link_to 'Add New Committee Member', committee_member_new_path %>
			<% @committee_members.each do |mem| %>
			<p>Name: <%= mem.name %></p>
			<%= button_to "Delete", delete_path(id: mem), :class => "button medium", :method => :patch %>
			<% end %>
		</div>

		<% end %>
	</div>

	<% end %>

</div>
<% end %>