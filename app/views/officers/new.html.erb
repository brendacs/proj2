<%= simple_form_for @officer, url: {action: "create"} do |p| %>
  <%= p.input :name %>
  <%= p.input :year %>
  <%= p.input :organization %>
  <%= p.collection_select :organization_id, Organization.order(:name), :id, :name %>
  <%= p.input :position %>
  <%= p.button :submit %>
<% end %>